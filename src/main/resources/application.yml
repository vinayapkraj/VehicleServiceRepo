spring:
    application:
        name: VehicleStatus
    

       
server:
    port: ${port:8088}
# EUREKA
eureka:
  instance:
    hostname: localhost
    port: 8081
    
  client:
    serviceUrl:
      defaultZone: http://localhost:8081/eureka/
    registerWithEureka: true
    fetchRegistry: true